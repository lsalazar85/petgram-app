import styled from "styled-components"

export const List = styled.ul`
    display: flex;
    flex-direction: row;
    overflow: scroll;
    padding: 0;
`

export const Item = styled.li`
    padding: 0 8px;
    list-style: none;
`
