import styled from 'styled-components'

export const Svg = styled.div`
    display: flex;
    flex-direction: row;
    width: 100%;
    justify-content: flex-start;
    & svg {
        width: 220px;
        margin-left: -20px;
    }
`
