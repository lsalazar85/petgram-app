import styled from 'styled-components'

export const Svg = styled.div`
    display: flex;
    flex-direction: row;
    width: 100%;
    justify-content: flex-start;
    & svg {
        width: 300px;
        margin-left: -20px;
    }
`
